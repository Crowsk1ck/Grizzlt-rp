GRIZZLY ROLE AUTH SYSTEM

FRONTEND:
npm install
npm run dev

BACKEND:
cd backend
npm install
npm run dev

API:
POST /api/check-role

BODY:
{
  "discordId":"USER_ID"
}

Only users with the configured Discord role can create contracts.